rootProject.name = 'mcengine-artificialintelligence'

// Define base directories for subprojects
def coreProjects = ['mcengine-artificialintelligence-api']
coreProjects.each {
    include ":$it"
}